#!/bin/bash

# #! is called she bang  it denotes the path of the interpreter and also ensure with that interpretuer if you execute

# any lines starts with # are comments and those are ignored during the executions.

ls
ls -a
ls -l
ls -A
